export type User = {
    name: string;
    email: string;
}

export type NavItemType = {
    href: string;
    text: string;
}

export type TechButtonType = {
    href: string;
    img: string;
    text: string;
}